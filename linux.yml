---
- name: Update Fedora Machine with Password
  hosts: linux
  gather_facts: yes
  become: yes
  become_user: root
  vars:
    ansible_become_pass: admin123  # Replace with your sudo password

  tasks:
    - name: Update the system
      dnf:
        name: "*"
        state: latest
